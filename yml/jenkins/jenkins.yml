version: "3"
services:
  jenkins:
    hostname: jenkins
    container_name: jenkins
    image: ${IMAGE}:${JENKINS_VERSION}
    privileged: true
    user: root
    volumes:
      - ${CURRENT_PATH}/volumes/jenkins/jenkins_home:/var/jenkins_home
      - /var/run/docker.sock:/var/run/docker.sock
    ports:
      - "18888:8080"
      - "15000:5000"
    env_file:
      - ${CURRENT_PATH}/env/jenkins/jenkins.env
    restart: always

    networks:
      - custom

networks:
  custom:
    external: true
