version: "3"
services:
  redis:
    hostname: redis
    container_name: redis
    image: redis:${REDIS_VERSION}
    volumes:
      - /Users/sunpeng/docker/volumes/redis/data:/data
      - /Users/sunpeng/docker/volumes/redis/logs:/logs
      - /Users/sunpeng/docker/volumes/redis/conf/redis.conf:/etc/redis/redis.conf
    ports:
      - "6379:6379"
    env_file:
      - "/Users/sunpeng/docker/env/redis/redis.env"
    restart: always
    command: ["redis-server","/etc/redis/redis.conf","--appendonly","yes"]

    networks:
      - custom

networks:
  custom:
    external: true
